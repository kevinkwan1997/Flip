import LogoService from '../../LogoService'

const Brand = ({ }) => {

    return (
        <div className="brand">
            <img className="brand-img"/>
        </div>
    )
}

export default Brand
